extern {
    func printf(s: string 0 ...): int
};

struct Hello {
  a = int, 
  b = int
}

func main(): int {
    let h =   init Hello {a = 1, b = 2};
    {h}.a = {h}.b + 1;
    printf("b = %d", {h}.a);
    return 0;
}
